/*
   The ML.Mat.Types.MUElement record type defines one element
   of a matrix-universe (mu) formatted matrix for use by ML.Mat
   functions.

*/

   IMPORT * FROM ML;
   ML.Mat.Types.MUElement mu_matrix;
