/*
   The ML.Mat.Types.VecElement record type defines one element
   of a vector for use by ML.Mat functions (y, the column
   index is always 1)

*/

   IMPORT * FROM ML;
   ML.Mat.Types.VecElement my_vector;
